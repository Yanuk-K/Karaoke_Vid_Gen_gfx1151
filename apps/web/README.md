# Web UI Starter (Placeholder)

Recommended build target for MVP:

- React + Vite
- Pages/components:
  - Upload panel
  - Job status panel
  - Karaoke preview player
  - Timing editor (line + word drag/nudge)

The backend is already available at `http://127.0.0.1:8000`.
